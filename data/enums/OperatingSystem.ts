// Optional: Add an enum for common operating systems

export enum OperatingSystem {
  Android = "Android",
  Windows = "Windows",
  Linux = "Linux",
  SteamOS = "Linux (Steam OS)",
  Custom = "Custom"
}
